.next
node_modules
playwright-report
test-results
src/generated

# Regenerated by `next build` on every run, and the file itself says it must not
# be edited. Formatting it means format:check fails after every build.
next-env.d.ts
package-lock.json
